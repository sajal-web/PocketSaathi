package com.sajalweb.pocketsaathi.ui.theme

import androidx.compose.ui.graphics.Color

// Primary - Deep Indigo (premium, trustworthy)
val Primary = Color(0xFF3A2E7C)           // Deep royal purple
val PrimaryLight = Color(0xFF5B4FA8)      // Softer purple for accents
val PrimaryDark = Color(0xFF231B54)       // Dark purple for contrast

// Secondary - Teal accent (fresh, sophisticated)
val Secondary = Color(0xFF00A896)         // Vibrant teal
val SecondaryLight = Color(0xFF33C4B2)    // Light teal
val SecondaryDark = Color(0xFF028072)     // Deep teal

// Tertiary - Gold accent (premium feel)
val GoldAccent = Color(0xFFD4AF37)        // Metallic gold
val GoldLight = Color(0xFFF5E6A3)         // Soft gold

// Backgrounds
val Background = Color(0xFFFCFBFF)        // Slight purple tint white (premium)
val Surface = Color(0xFFF8F6FE)           // Soft lavender-white
val CardBg = Color(0xFFFFFFFF)            // Pure white cards with subtle shadow

// Semantic colors
val GreenPositive = Color(0xFF00A86B)     // Emerald green (success)
val AmberCaution = Color(0xFFF5A623)      // Warm amber (warning)
val RedWarning = Color(0xFFE53E3E)        // Soft red (error)
val InfoBlue = Color(0xFF3182CE)          // Calm blue (neutral info)

// Text
val TextPrimary = Color(0xFF1A1A2E)       // Almost black with blue undertone
val TextSecondary = Color(0xFF5A5A7A)     // Muted navy-gray
val TextTertiary = Color(0xFF8E8EA8)      // Light gray for hints
val TextOnPrimary = Color(0xFFFFFFFF)     // White text on primary
val TextOnSecondary = Color(0xFFFFFFFF)   // White text on secondary

// Borders & Dividers\
val Divider = Color(0xFFE8E6F0)           // Soft lavender-gray
val BorderLight = Color(0xFFF0EEF8)       // Very light border